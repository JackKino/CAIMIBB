<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="App_SystemBar_Style">
        <item name="android:clipToPadding">true</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:background">@color/color_app_base_color</item>
    </style>

    <style name="Loading_Dialog_Style" parent="android:style/Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFrame">@null</item>
    </style>

    <style name="Loading_Dialog_WithBackGround_Style" parent="android:style/Theme.Dialog">
        <!-- 背景颜色及透明程度 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 是否有标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否模糊 -->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFrame">@null</item>
    </style>

    <style name="myProgressBarStyleLarge">
        <item name="android:indeterminateDrawable">@drawable/loading_shape</item>
        <item name="android:minWidth">35dp</item>
        <item name="android:maxWidth">35dp</item>
        <item name="android:minHeight">35dp</item>
        <item name="android:maxHeight">35dp</item>
    </style>

    <style name="myProgressBarStyleSmall">
        <item name="android:indeterminateDrawable">@drawable/loading_shape</item>
        <item name="android:minWidth">25dp</item>
        <item name="android:maxWidth">25dp</item>
        <item name="android:minHeight">25dp</item>
        <item name="android:maxHeight">25dp</item>
    </style>

    <style name="MainMenuStyle">
        <item name="android:drawablePadding">@dimen/main_menu_drawable_padding</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:textColor">@drawable/selector_main_menu_textcolor</item>
        <item name="android:textSize">@dimen/main_menu_textsize</item>
        <item name="android:button">@null</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="MINE_ITEM_TEXTSTYLE">
        <item name="android:drawablePadding">@dimen/my_item_drawable_padding</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@color/color_333333</item>
        <item name="android:textSize">@dimen/my_top_ticket_textsize</item>
        <item name="android:paddingLeft">@dimen/my_item_h_padding</item>
        <item name="android:paddingRight">@dimen/my_item_h_padding</item>
        <item name="android:drawableRight">@mipmap/icon_arrow_right_large</item>
        <item name="android:background">@drawable/selector_trans_item</item>
    </style>
</resources>
